{
  "name": "@ghost-atlas/mind-as-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test dist/**/*.test.js",
    "check": "npm run typecheck && npm run build && npm test"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}